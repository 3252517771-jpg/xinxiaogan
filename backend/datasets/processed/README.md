# Processed Datasets

This directory stores generated training tables created from the raw public datasets.

- Generated by: `backend/ml_models/train.py`
- Primary file: `risk_training_dataset.csv`
- Contents: normalized S1 feature columns plus the derived `risk_label`

Processed CSV files are local build artifacts and are not committed.
