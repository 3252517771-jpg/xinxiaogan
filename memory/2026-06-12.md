# memory/2026-06-12.md

## 里程碑完成: S2 - 行为识别

**确认人：** 小洋裙
**确认时间：** 2026-06-12
**Commit：** `9dbe9cf`

### 本次完成内容
- 新增行为识别规则引擎，覆盖熬夜、睡眠不足、缺餐、久坐、高压、体征异常 6 类行为模式。
- 扩展五个健康提交接口，提交后返回 `behavior_tags`。
- 首页行为标签接入真实后端结果，不再以默认 mock 作为验收路径。

### 当前已知问题
- `vite build` 有主包体积偏大提示，不影响 S2 功能验收。

### 下一阶段
- 进入 S3：AI 个性化建议。

---

## 里程碑完成: S3 - AI 个性化建议

**确认人：** 小洋裙
**确认时间：** 2026-06-12
**Commit：** `cae5cad`

### 本次完成内容
- 后端接入 DeepSeek API，新增 Privacy Guard 脱敏与 fallback 模板降级。
- 扩展五个详情页真实提交链路，`POST /api/health/sleep|diet|exercise|stress|risk` 返回 `ai_advice`。
- 个人主页新增 AI 建议开关，用户关闭后直接使用规则模板。
- 修正新用户空状态，首页、详情页、个人主页、历史记录不再展示模板健康数据。
- 清空本地数据库并创建模板用户 `template_user`，用于人工验收完整测试数据。
- 更新 AGENTS.md：验收阶段改为简单业务逻辑验证 + 人工验证。

### 验收证据
- `npm run build` 通过。
- 后端健康检查 `/health` 返回 `{"status":"ok","service":"xinxiaogan-backend"}`。
- 人工验收：空用户逻辑与模板用户数据已确认通过。

### 当前已知问题
- `vite build` 有主包体积偏大提示，不影响 S3 功能验收。

### 下一阶段
- 进入 S4：推送通知。

---

## 里程碑完成: S4 - 推送通知

**确认人：** 小洋裙
**确认时间：** 2026-06-12 03:00
**Commit：** `bc776fe`

### 本次完成内容
- 接入 Server 酱推送服务，支持 SendKey 校验、测试推送与失败降级提示。
- 扩展个人主页资料配置，支持保存 SendKey、开启或关闭微信自动推送、发送测试推送。
- 新增唯一允许的新端点 `POST /api/user/push-test`，Profile 响应只返回绑定状态，不明文回显 SendKey。
- 接入 APScheduler，注册每日 21:00 未记录提醒、每日 22:00 风险提醒、每周日 22:30 正向鼓励。
- 新增 `output/s4-push-rules-smoke.py`，用临时库和假发送器验证三类自动推送规则与调度任务注册。
- 修正首页真实数据同步：ScrollStack 卡片读取 `/api/health/latest`，近 7 天趋势图读取 `/api/health/trend?days=7`。

### 验收证据
- `npm run build` 通过。
- Server 酱真实测试推送返回成功，提示已加入队列。
- `output/s4-push-rules-smoke.py` 通过：`daily_reminder_count=1`、`risk_alert_count=1`、`encouragement_count=1`。
- `/api/health/trend?days=7` 冒烟返回真实趋势点，行为接口返回真实规则结果。
- 人工验收已放行。

### 当前已知问题
- `vite build` 有主包体积偏大提示，不影响 S4 验收。

### 下一阶段
- 进入 S5：集成验证。

---

## 里程碑完成: S5 - 集成验证

**确认人：** 小洋裙
**确认时间：** 2026-06-12 15:06
**Commit：** `3c43db0`

### 本次完成内容
- 对照 `docs/v2.0-ai-companion-design.md`、`docs/后端技术方案.md`、`docs/数据库设计.md` 和 AGENTS.md 完成 v2.0 S1-S4 最小集成验收。
- 新增 `output/s5-integration-smoke.py`，使用临时 SQLite 数据库验证真实后端闭环，不污染当前验收数据库。
- 验证新用户注册后首页数据源为空，提交运动与风险记录后 `/api/health/latest`、`/api/health/trend?days=7` 可同步真实数据。
- 验证运动提交返回行为标签与 AI 建议，风险提交返回 ML 风险字段、风险提醒状态与 AI 建议。
- 确认前端默认真实 `/api` 路径，mock 仅保留在显式 `npm run dev:mock` / `VITE_USE_MOCK_API=true`。

### 验收证据
- `npm run build` 通过。
- `output/s5-integration-smoke.py` 通过：`health_ok=true`、`new_user_latest_empty=true`、`latest_sync_after_submit=true`、`trend_has_submitted_scores=true`、`risk_has_ml_fields=true`。
- 前后端入口可访问：`http://127.0.0.1:5174` 返回 200，`http://127.0.0.1:8000/health` 返回 ok。
- 人工验收已放行，可以提交 S5。

### 当前已知问题
- `vite build` 有主包体积偏大提示，不影响 S5 验收。
- S5 冒烟脚本触发 sklearn feature names warning，但 ML 推理字段返回正常，不阻断验收。

### 下一阶段
- v2.0 S1-S5 首轮集成验证完成；M12/M13 与后续版本迭代按计划延后。

---

## 里程碑完成: M13 - 部署上线

**确认人：** 小洋裙
**确认时间：** 2026-06-12 18:57
**Commit：** `6d38de5`

### 本次完成内容
- 创建 Gitee 私有镜像仓库 `zcf2335061227/xinxiaogan`，按“本地 → GitHub 主仓 + Gitee 镜像，ECS → 从 Gitee 拉取”完成部署链路。
- ECS 从 Gitee clone/pull 到 `/opt/xinxiaogan`，当前线上提交为 `6d38de5b7225e642fd14247bb4c2a57a2d394f4a`。
- 在 ECS 安装 Node v22.12.0 并执行 `npm ci && npm run build`，Nginx 托管 `/opt/xinxiaogan/dist`。
- 后端使用 `/opt/xinxiaogan/.venv` 运行 Uvicorn，Supervisor 守护 `xinxiaogan`，监听 `127.0.0.1:8001`。
- 生产数据库使用 SQLite：`/opt/xinxiaogan/xinxiaogan.db`。
- 更新 AGENTS.md：新增 M13 必须从 Gitee 拉取、不得打包上传、不得影响同服务器 `dji-rental` 的规则。
- 保持同服务器 `dji-rental` 隔离运行，不改动 `/opt/dji-rental/`、`dji-rental` Supervisor 配置、Nginx 站点和 `127.0.0.1:5000` 端口。

### 验收证据
- `npm run build` 本地通过；ECS 上 `npm ci && npm run build` 完成并生成 `dist/index.html`。
- `supervisorctl status`：`xinxiaogan` 为 `RUNNING`，`dji-rental` 仍为 `RUNNING`。
- `https://xinxiaogan.moomlostdress.cn/` 返回 200。
- `http://127.0.0.1:8001/health` 返回 `{"status":"ok","service":"xinxiaogan-backend"}`。
- `http://127.0.0.1:8001/db/ping` 返回 SQLite 连接正常。
- `https://xinxiaogan.moomlostdress.cn/api/auth/me` 未登录返回 401，证明 Nginx `/api/` 反代到 FastAPI。
- 线上最小业务闭环通过：注册验收用户 → 鉴权成功 → 提交风险记录 → 返回 `risk_level`、`risk_probability`、`risk_alert`、`ai_advice` → `/api/health/latest` 同步风险记录。
- `dji-rental` 不受影响：`http://127.0.0.1:5000/` 返回 200，`https://moomlostdress.cn/` 返回 200。
- `nginx -t` 通过。

### 当前已知问题
- Vite 构建仍有主包体积偏大 warning，不影响上线可用性。
- 线上 sklearn 版本与本地训练版本存在 joblib 反序列化 warning，但模型可加载并已返回风险预测字段；后续可统一训练环境版本消除 warning。

### 下一阶段
- 首版程序已上线；M12 开发维护模式与后续版本迭代按计划后置。
