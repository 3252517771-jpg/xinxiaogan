# memory/2026-06-12.md

## 里程碑完成: S2 - 行为识别

**确认人：** 小洋裙
**确认时间：** 2026-06-12
**Commit：** `07627bf`

### 本次完成内容
- 新增 `behavior.py`，实现 6 条行为识别规则：熬夜、睡眠不足、缺餐、久坐、高压、体征异常
- 新增 `score_service.py`，统一承接五个维度的评分计算
- 补齐 `POST /api/health/sleep|diet|exercise|stress|risk` 的真实提交链路，并返回 `behavior_tags`
- 首页在趋势图下方新增行为识别内联标签展示
- 补充 S2 验证产物：规则命中审计、五维接口返回审计、首页行为标签截图脚本

### 当前已知问题
- `vite build` 仍提示前端主包体积偏大，但不影响当前 S2 功能验收

### 下一阶段
- 即将进入 S3：AI 个性化建议

---

## 里程碑完成: S3 - AI 个性化建议

**确认人：** 小洋裙
**确认时间：** 2026-06-12
**Commit：** `cae5cad`

### 本次完成内容
- 后端接入 DeepSeek API：新增 Privacy Guard 脱敏、AI 建议生成与 fallback 模板降级
- 扩展五个详情页真实提交链路，`POST /api/health/sleep|diet|exercise|stress|risk` 返回 `ai_advice`
- 个人主页新增 AI 建议开关字段读取与保存，用户关闭后回退规则模板
- 前端默认走真实 `/api`，Mock 仅保留在显式 `npm run dev:mock`
- 修正登录态校验，旧 token 不再造成详情页假登录后 401
- 修正新用户空状态：个人资料、首页卡片、趋势图、详情页摘要、历史记录、导出模块不再展示模板健康数据
- 清空本地数据库并创建模板账号 `template_user`，用于人工验收完整测试数据
- 更新 AGENTS.md：验收阶段改为简单业务逻辑验证 + 人工验证，避免过长验真链路

### 验收证据
- `npm run build` 通过
- `http://127.0.0.1:8000/health` 返回 `{"status":"ok","service":"xinxiaogan-backend"}`
- 人工验收：空用户逻辑与模板用户数据已确认通过

### 当前已知问题
- `vite build` 仍提示前端主包体积偏大，但不影响当前 S3 功能验收
- 首页趋势图当前仍是空状态提示，后续如需展示近 7 天曲线，需要继续扩展真实趋势接口

### 下一阶段
- 即将进入 S4：推送通知
