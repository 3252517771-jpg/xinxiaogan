# memory/2026-06-10.md

## 里程碑完成: M1 - 项目脚手架搭建

**确认人：** 小洋裙
**确认时间：** 2026-06-10 21:37
**Commit：** `90e1ca5`

### 本次完成内容
- Git 仓库初始化 + .gitignore。
- Vite + React 19 + TypeScript 6 脚手架。
- Tailwind CSS 3.4 自定义主题：forest / dimension 色、字体、玻璃 Token。
- 完整目录结构：src/pages/features/components/ui/background/ip/transitions/chart/layout + services/mock/hooks/types/config/utils/styles + backend/docs。
- 依赖安装：react-router-dom / GSAP / Framer Motion / Recharts / lucide-react。
- 6 张图片 + 21 个视频素材复制到 public/assets/。
- 入口文件：src/main.tsx / src/App.tsx / src/index.css / src/vite-env.d.ts。
- 配置文件：vite.config.ts / tsconfig.json / tailwind.config.js / postcss.config.js。
- package.json：包含 dev / build / preview 脚本。
- `npm run dev` 启动正常。
- `tsc --noEmit` 零错误通过。
- GitHub 仓库创建并推送：https://github.com/3252517771-jpg/xinxiaogan

### 当前已知问题
- React Bits 组件尚未安装，按需通过 shadcn CLI 安装。
- 所有组件目前为空壳，需在后续 Sprint 填充。

### 下一阶段
- 即将进入 M2：目录结构 + 空壳组件落地，GlassCard / PillButton / SlimTag / MergedShape 等核心 UI 组件完成。

## 里程碑完成: M2 - 目录结构 + 空壳组件

**确认人：** 小洋裙
**确认时间：** 2026-06-10 21:58
**Commit：** `4caf8d1`

### 本次完成内容
- 补齐 `src/pages/`、`src/features/`、`src/components/`、`src/config/`、`src/types/`、`src/services/`、`src/store/`、`src/hooks/`、`src/utils/`、`src/styles/` 目录骨架。
- 新增 7 个页面空壳：首页、作息、饮食、运动、压力、风险、个人主页。
- 接通 7 条前端路由：`/`、`/sleep`、`/diet`、`/exercise`、`/stress`、`/health-risk`、`/profile`。
- 新增基础 UI、背景层、IP、图表、布局、页面过渡等占位组件。
- 新增 Mock 数据、基础类型、路由配置、素材 URL 管理、评分工具等 M2 必需落点。

### 当前已知问题
- 仍处于空壳阶段，组件仅用于路由空转和结构占位，M3 再完善全局样式与基础 UI 质感。
- React Bits 组件尚未按需安装，后续 Sprint 使用到具体组件时再引入。

### 下一阶段
- 即将进入 M3：全局样式 + 基础 UI 组件。

## 里程碑完成: M3 - 全局样式 + 基础UI组件

**确认人：** 小洋裙
**确认时间：** 2026-06-10 22:53
**Commit：** `a928794`

### 本次完成内容
- 完善全局样式 token：森林暗色背景、玻璃质感、阴影、动效、字体和 1920 桌面优先基础约束。
- 打磨基础 UI 组件：GlassCard / PillButton / SlimTag / ScoreBadge / MergedShape / FieldInput / StarRating / Toast。
- 更新背景层、导航、页面头、GooeyNav、IPVideoPlayer 和图表占位组件，使玻璃 Token 在页面中生效。
- 将首页和 5 个详情页表单空壳接入基础 UI 质感，修复部分可见中文乱码文案。
- 生成 Playwright 截图完成视觉自检，截图产物保留在 `output/playwright/`，不纳入里程碑提交。
- 按用户确认，将 `AGENTS.md` 纳入 git 跟踪。

### 当前已知问题
- React Bits 真实组件尚未接入，M4 首页开发时再按需引入。
- 图表仍为 M3 静态占位，M4/M5 再接入 Recharts 与 Mock 数据。

### 下一阶段
- 即将进入 M4：首页可运行。

## 里程碑完成: M4 - 首页可运行

**确认人：** 小洋裙
**确认时间：** 2026-06-10 23:56
**Commit：** `c54859a`

### 本次完成内容
- 首页接入 `H-01.mp4` 视频背景，并以 `首页图.png` 作为 poster/静态兜底。
- 完成首页登录前锁定：未登录仅展示 PillNav、标语与登录模态，不展示卡片、趋势图和个人入口。
- 完成 Mock 登录解锁：首页展示 PillNav 问候、综合评分、标语、5 张健康维度卡、趋势图和个人中心入口。
- 完成 5 张 MergedShape 风格维度卡，包含作息/饮食/运动/压力/风险的状态、分数和详情入口。
- 使用 Recharts 接入近 7 天趋势面积图，并支持综合/作息/饮食/运动/压力/风险标签切换。
- 补充 M3/M4 Playwright 验证截图到 `output/playwright/`，作为里程碑证据链。
- 更新 `AGENTS.md` 交付验证标准，新增“证据链完善吗？”检查项。

### 当前已知问题
- 首页 ScrollStack / PillNav / CountUp / SplitText / FadeContent / BorderGlow 为项目内实现的等效效果，尚未接入 React Bits 官方组件源码。
- Vite 构建存在 chunk 体积警告，主要来自 Recharts 进入主包；当前不影响 M4 验收。

### 下一阶段
- 即将进入 M5：API Mock 层完成。
