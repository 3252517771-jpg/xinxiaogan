# memory/2026-06-11.md

## 里程碑完成: M5 - API Mock 层完成

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 08:03  
**Commit：** `1bced48`

### 本次完成内容
- 完成统一 Mock API 请求层，开发环境 `request<T>()` 走 Mock 路由，生产环境保留真实 fetch 分支。
- 覆盖 `/auth/register`、`/auth/login`、5 个健康维度 GET/POST、`/health/overall`、`/health/trend`、`/user/profile`、`/health/history`、`/admin/verify` 等文档接口。
- 登录模态接入 Mock API，登录后写入 token 并解锁首页。
- 修正 M4 遗留：首页 ScrollStack 接入真实 `lenis` 动效、趋势图居中、PillNav 顶部导航、首屏 Split Text 标题、背景层取消暗角、卡片底色增强。
- 移除详情页前景 IP 状态机调试卡，保留到 M9 再正式接入 IP 视频状态机。
- 输出验证证据到 `output/playwright/`，包括 Mock API 审计、首页解锁、ScrollStack、详情页无前景 IP 卡片截图。

### 当前已知问题
- 详情页整体布局仍是 M5 可运行基线，不作为 M8 最终布局验收；M8 需严格按 `项目立项文档.md` 重排。
- 构建存在 Vite chunk size warning，当前不影响 M5 验收。

### 下一阶段
- 即将进入 M6：数据库连接（后端）

---

## 里程碑完成: M6 - 数据库连接（后端）

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 08:46  
**Commit：** `cb58654`

### 本次完成内容
- 新增 `backend/` FastAPI 后端最小骨架，提供 `/health` 服务健康检查与 `/db/ping` 数据库连通性检查。
- 新增 SQLAlchemy async 数据库连接层，M6 验收 SQL 为 `SELECT 1`。
- 根据实际环境采用 SQLite 作为开发数据库，默认连接 `backend/dev.db`，保留 `DATABASE_URL` 覆盖能力，后续部署可切换 PostgreSQL。
- 参考 Anaconda 环境说明，明确后端固定使用 Conda 环境 `D:\conda-envs\claude-code`，并写入 `AGENTS.md`。
- 新增 M6 验证脚本与证据文件：`output/playwright/m6-db-connection-audit.py`、`output/playwright/m6-db-connection-verification.json`。

### 验证结果
- 使用 `D:\conda-envs\claude-code` 环境完成后端验收。
- `/health` 返回 `status=ok`。
- `/db/ping` 返回 `ok=true`、`result=1`、`database_url_driver=sqlite+aiosqlite`。
- `output/playwright/m6-db-connection-verification.json` 中 `passed=true`。
- `python -m compileall backend\app output\playwright\m6-db-connection-audit.py` 通过。
- `npm run build` 通过，仅保留已知 Vite chunk size warning。

### 当前已知问题
- 当前 M6 只验证开发数据库连接与第一条 SQL，不包含真实业务 CRUD。
- 本机无 PostgreSQL 服务；开发期按用户确认改用 SQLite。

### 下一阶段
- 即将进入 M7：登录/注册全流程

---

## 里程碑完成: M7 - 登录/注册全流程

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 09:15  
**Commit：** `b989598`

### 本次完成内容
- 后端新增认证最小闭环：`users` / `user_profiles` 建表、注册、登录、JWT 签发与 `/api/auth/me` 校验。
- 注册成功自动创建默认用户资料，并返回 bearer token；登录使用 bcrypt 校验密码并返回 JWT。
- 前端升级 AuthStore 为 Context，登录/注册成功后保存 token 与用户名，首页随 token 解锁。
- 新增 AuthGuard，未登录访问详情页或个人主页会回到首页登录，登录后可进入受保护页面。
- `src/services/api.ts` 增加 `VITE_USE_MOCK_API=false` 开关，开发默认仍保留 Mock，M7 验收可切真实 FastAPI。
- 新增 M7 API 与浏览器验证脚本，截图和 JSON 证据输出到 `output/playwright/`。

### 验证结果
- 人工验收已通过。
- `output/playwright/m7-auth-flow-verification.json` 中 `passed=true`。
- `output/playwright/m7-auth-ui-verification.json` 中 `passed=true`。
- 生成截图：`m7-auth-locked-home.png`、`m7-auth-unlocked-home.png`、`m7-auth-unlocked-sleep.png`。
- `python -m compileall backend\app output\playwright\m7-auth-flow-audit.py` 通过。
- `npm run build` 通过，仅保留已知 Vite chunk size warning。

### 当前已知问题
- M7 只完成认证与页面解锁，健康数据、趋势、个人主页和 admin 等业务接口仍按 M5 Mock 层运行。

### 下一阶段
- 即将进入 M8：5 个详情页完成

---

## 里程碑完成: M8 - 5个详情页完成

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 11:20  
**Commit：** `c6c8dd9`

### 本次完成内容
- 严格对照 `项目立项文档.md` 5.3-5.7 和 `docs/前端技术方案与实施计划.md` Sprint 3，完成作息、饮食、运动、压力、风险 5 个详情页布局。
- 作息页、运动页接入 React Bits `MagicBento` 数据区；运动页卡片宽度已调整为与作息页一致。
- 5 个详情页底部导航改为 React Bits `Dock`，支持详情页之间切换。
- 饮食页、压力页、风险页组件区域接入 React Bits `TiltedCard` 包装。
- 5 个详情页表单补齐文档字段，并通过 Mock POST 提交显示“已记录，当前评分 xx”的反馈。
- 修复详情页中文乱码，移除 Lanyard 试验改动，个人主页恢复原本直接展示。
- 输出 M8 验证材料到 `output/playwright/`，包括 5 个详情页截图与 `m8-detail-pages-verification.json`。

### 验证结果
- `npm run build` 通过，包含 `tsc --noEmit` 与 Vite build。
- 检查无 `console.log`、无冲突标记、无 `any` 类型扫描命中。
- 已按流程推送人工验收，用户确认“没问题就提交 m8 / 确认提交”。

### 当前已知问题
- Vite 仍提示 chunk size warning，属于既有非阻塞警告。
- IP 视频状态机仍按计划留到 M9 正式完成。

### 下一阶段
- 即将进入 M9：IP视频状态机

---

## 里程碑完成: M9 - IP视频状态机

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 12:59  
**Commit：** `e81b40e`

### 本次完成内容
- 严格对照 `项目立项文档.md` 4.4、5.3-5.7 与 `docs/前端技术方案与实施计划.md` Sprint 4，补全 5 个详情页的 IP 视频状态机链路。
- 重写 `useVideoState`，将详情页背景视频切换为 `good / calm / bad / feedback` 四态，并支持反馈态结束后自动恢复到评分对应状态。
- `BackgroundLayer` 支持按场景播放视频、`calm` 滤镜、`bad` 单次停帧、反馈视频结束回调。
- 作息、饮食、运动、压力、风险 5 个表单提交成功后接入 Mock 返回分数，页面分数与背景视频状态同步更新。
- 首页 ScrollStack 卡片进入详情页时接入对应 `T-0X` 过渡视频；根据人工反馈，详情页之间切换恢复为直切，不再播放过渡动画。
- 新增 `src/store/transitionStore.ts` 作为首页进入详情页的轻量过渡状态管理。

### 验证结果
- `npm run build` 通过，包含 `tsc --noEmit` 与 Vite build。
- 人工验收已通过，用户确认“这边运行正常”并确认提交 M9。
- 本地开发入口 `http://127.0.0.1:4173` 已验证可访问。

### 当前已知问题
- Vite 仍提示 chunk size warning，属于既有非阻塞警告。
- M9 浏览器自动化验收脚本已编写，但本机 Playwright 浏览器运行时未完整落地，因此本次提交以人工验收和构建通过为主证据。

### 下一阶段
- 即将进入 M10：个人主页

---

## 里程碑完成: M10 - 个人主页

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 13:18  
**Commit：** `e9e6e8e`

### 本次完成内容
- 严格对照 `项目立项文档.md` 5.8 与 `docs/前端技术方案与实施计划.md` Sprint 5，完成人个主页的资料编辑、历史记录、数据导出与 API 预留模块。
- 重做 `ProfilePage` 整体结构，形成独立头部、左侧资料与历史区、右侧导出与预留区的个人中心布局，并弱化背景层使页面更接近文档中的独立淡灰氛围。
- `ProfileForm` 接入 `/user/profile` Mock 保存流程，资料保存后显示反馈提示。
- `HistoryList` 支持五维筛选与“查看更多”交互，可按维度过滤历史记录。
- `DataExport` 新增真实 CSV Blob 导出流程，下载文件名为 `xinxiaogan-profile-export.csv`。
- 输出 M10 验证材料到 `output/playwright/`，包含页面截图、自动化验证脚本、验证 JSON 与导出 CSV 样例。

### 验证结果
- `npm run build` 通过，包含 `tsc --noEmit` 与 Vite build。
- `output/playwright/m10-profile-verification.json` 验证通过：页面可打开、资料保存成功、历史记录筛选“饮食”后仅保留对应记录、CSV 导出成功并生成文件。
- 页面截图产物：`output/playwright/m10-profile-audit-page.png`
- 导出样例产物：`output/playwright/downloads/m10-profile-export.csv`
- 已按流程推送人工验收，用户确认提交 M10。

### 当前已知问题
- Vite 仍提示 chunk size warning，属于既有非阻塞警告。
- 当前个人主页保存与历史数据仍基于 Mock 层，后续接真实后端时只需替换 API 端。

### 下一阶段
- 即将进入 M11：GooeyNav 底部导航

---

## 里程碑完成: M11 - 详情页底部导航与首版收口

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 21:58  
**Commit：** `39fc992`

### 本次完成内容
- 按人工确认结果，详情页底部导航最终采用 React Bits `Dock` 方案，完成 5 个详情页之间的固定底部导航切换。
- 补充 M11 导航验收脚本与截图证据，验证作息、饮食、运动、压力、风险 5 个详情页均可显示底部导航并保持切换能力。
- 优化详情页 IP 状态机播放流程：首页过渡视频结束后，`good / calm` 状态视频改为按 10 秒周期重播，不再无缝原生循环。
- 保持 `bad` 状态单次播放后定格、`feedback` 状态单次反馈后恢复评分态的既有语义。
- 补充状态机验收脚本与证据，验证 `good / calm / bad / feedback` 四种状态均命中正确视频资源。
- 用户确认首版程序开发至此收口，M12、M13 留待后续版本迭代开发。

### 当前已知问题
- `npm run build` 仍存在既有 Vite chunk size warning，但不影响首版功能验收与运行。

### 下一阶段
- 后续版本再进入 M12：开发维护模式

---

## 里程碑完成: S1 - ML 风险预测

**确认人：** 小洋裙  
**确认时间：** 2026-06-11 23:40  
**Commit：** `b56d586`

### 本次完成内容
- 重新审阅 v2.0 文档并按 S1 要求，将风险模型训练从内联样本改为真实公开数据集训练。
- 采用 CDC NHANES 2017-2018 官方公开数据，下载并使用 `BPX_J.XPT`、`BMX_J.XPT`、`GLU_J.XPT`、`TCHOL_J.XPT`、`MCQ_J.XPT` 五份数据组装训练集。
- 新增 `backend/ml_models/train.py`，自动完成公开数据下载、字段映射、三分类标签生成、模型训练与 `joblib` 产物导出。
- 生成训练产物 `backend/ml_models/risk_classifier.joblib`、`backend/ml_models/risk_scaler.joblib`，并输出 `backend/datasets/processed/risk_training_dataset.csv`。
- 收紧 `risk_predictor.py` 的启动逻辑：缺少真实模型文件时直接报错，不再静默回退到演示样本模型。
- 补齐训练与接口验证证据：`output/s1-training-summary.json`、`output/s1-risk-prediction-verification.json`。

### 当前已知问题
- `npm run build` 仍有既有 Vite chunk size warning，不影响 S1 验收与运行。
- PowerShell 终端读取中文时仍可能显示乱码，但仓库文件以 UTF-8 写入，不影响实际内容。

### 下一阶段
- 即将进入 S2：行为识别
